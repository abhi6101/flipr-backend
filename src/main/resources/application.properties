spring.application.name=backend

# --- SUPABASE TRANSACTION POOLER (IPv4 Compatible) ---
# Host: aws-0-ap-southeast-1.pooler.supabase.com (From your screenshot)
# Port: 6543
# Database: postgres
# Option: prepareThreshold=0 is REQUIRED for Transaction Poolers
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0

# Your Username (From screenshot)
spring.datasource.username=postgres.byncekvtlzuiograkafy

# Your Password (The one you set)
spring.datasource.password=MySecurePass12

spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Connection Pool Settings (Optimized for Supabase Pooler)
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.max-lifetime=20000
spring.datasource.hikari.idle-timeout=15000